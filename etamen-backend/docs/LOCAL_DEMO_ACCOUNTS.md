# Local Demo Accounts

These are fake demo accounts generated by `PilotDemoSeeder` for local/internal testing only. They are not production credentials and must not be used with real users or real data.

Common demo password:

```text
Password1234
```

## Core Demo Accounts

| Email | Role | Workspace | What to Test |
| --- | --- | --- | --- |
| `pilot.admin@example.test` | Platform admin | Platform Admin | Admin dashboard, payment reviews, provider approvals, support, refunds, disputes, audit log. |
| `pilot.patient@example.test` | Patient | Patient | Patient app, doctor booking, radiology, gym, coach, support/refund/dispute. |
| `pilot.doctor@example.test` | Doctor/provider owner | Doctor provider | Doctor workspace, dashboard, appointments. |
| `pilot.hospital@example.test` | Provider owner | Hospital provider | Hospital dashboard, hospital appointments, departments, linked doctors. |
| `pilot.radiology@example.test` | Provider owner | Radiology provider | Radiology orders and provider operations. |
| `pilot.pharmacy@example.test` | Pharmacy admin/provider | Pharmacy provider | Pharmacy MVP provider pages and smoke checks. |
| `pilot.lab@example.test` | Lab admin/provider | Lab provider | Lab MVP provider pages and smoke checks. |
| `pilot.gym@example.test` | Provider owner | Gym provider | Gym dashboard, bookings, plans, classes. |
| `pilot.fitness.coach@example.test` | Provider owner | Fitness coach provider | Coach bookings, availability, session types, packages. |
| `pilot.nutrition.coach@example.test` | Provider owner | Nutrition coach provider | Coach operations with non-medical nutrition guardrails. |
| `pilot.provider.staff@example.test` | Limited provider staff | Limited doctor provider staff | Limited staff read-only/403 permission guard. |

## Local/Testing QA Short Accounts

These are created only when the Laravel environment is `local` or `testing`.

| Email | Role | Workspace | What to Test |
| --- | --- | --- | --- |
| `a@b.co` | Platform admin | Platform Admin | Fast local admin QA. |
| `p@b.co` | Patient | Patient | Fast local patient QA. |
| `d@b.co` | Provider owner | Doctor provider | Fast local provider QA. |

## Rules

- Local/demo only.
- No real user data.
- Do not document these as production credentials.
- Do not seed short QA accounts outside local/testing.
- Staging and production must use their own approved credential process.
