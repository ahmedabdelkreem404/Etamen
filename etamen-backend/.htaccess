<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send all requests from public_html to Laravel's public directory.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
